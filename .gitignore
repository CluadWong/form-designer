# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

node_modules
dist
dist-app
dist-ssr
*.local

# npm 私有源凭据（含 token，绝不入库）
.npmrc

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AI 协作工具的会话记忆/配置（非项目内容，不入库）
.workbuddy/
.claude/
.cursor/

# Vite / Vitest 临时配置缓存（esbuild 转译 *.config.ts 的副产物，运行中断会残留，可安全忽略/删除）
*.timestamp-*.mjs

# 发布包消费端验证脚手架（临时，含 npm pack 产物与假 node_modules，不入库）
.tmp-erp-check/

# 设计器「导出文件」生成的调试样例（前缀 + 时间戳，非项目内容，不入库）
src/dev/ticket-schema-v2-*.json
src/dev/ticket-fill-data-*.json
